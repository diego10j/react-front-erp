export type IgetTrnCliente= {
  ide_geper: number;
  fechaInicio: Date | null;
  fechaFin: Date | null;
};

export type IgetSaldoCliente= {
  ide_geper: number;
};

export type IgetProductosCliente= {
  ide_geper: number;
};
